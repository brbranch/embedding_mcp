---
description: 開発フローを管理するスキル（TODO管理、エージェント呼び出し）
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, Task, AskUserQuestion
---

# 開発フロー管理スキル (Dev Flow Skill)

TODO.mdに基づいて開発フローを管理し、各エージェントを呼び出す。

## 使用方法

```
/dev-flow
```

- 🚧 着手中のタスクがあれば → 続きから再開
- 着手中がなければ → 最初の未着手タスク `[ ]` を開始

---

## ステップ一覧（7ステップ）

| Step | 内容 | 担当 | レビュー |
|------|------|------|----------|
| 1 | Worktree作成 | - | - |
| 2 | 実装計画 + 要件トレーサビリティ表 | 設計者 (Opus) | - |
| 3 | 設計レビュー | Copilot | ✅ |
| 4 | TDD実装 | 実装者 (Sonnet) | - |
| 5 | E2E + README | 仕上げ担当 (Sonnet) | - |
| 6 | 全体レビュー | Copilot | ✅ |
| 7 | リリース | - | - |

---

## 品質担保（必須）

| 品質要件 | 担保メカニズム |
|---------|---------------|
| デグレード検知 | `go test ./...` を各ステップ完了時に実行 |
| 要件網羅 | 要件トレーサビリティ表（Step 2作成、Step 6確認） |
| 設計改変防止 | Copilotレビュー（Step 3, 6） |

### 要件トレーサビリティ表（Step 2で作成必須）

```markdown
| 要件ID | TODO項目 | テストケース | 実装箇所 |
|--------|----------|--------------|----------|
| REQ-1 | memory.add_note実装 | TestAddNote_* | internal/service/memory.go:AddNote |
```

- Step 3: 表の完全性チェック
- Step 6: 「実装箇所」列が全て埋まっていることを確認

---

## 各ステップ詳細

### Step 1: Worktree作成

1. feature ブランチ作成 → worktree 作成
2. 進捗ファイル作成 (`docs/progress/phase{N}-task{M}.json`)
3. TODO.md を `[ ]` → `🚧` に更新

### Step 2: 実装計画作成

**担当**: 設計者 (Opus)

1. TODO.md / requirements/ から要件確認
2. `docs/plans/phase{N}-task{M}.md` に実装計画作成
3. **要件トレーサビリティ表を作成**（必須）
4. コミット & プッシュ → `go test ./...` 確認

### Step 3: 設計レビュー

**担当**: Copilot

**観点**: TODO/仕様との整合、要件トレーサビリティ表の完全性、テストケース網羅性

LGTM後、修正あればコミット & プッシュ → `go test ./...` 確認

### Step 4: TDD実装

**担当**: 実装者 (Sonnet)

1. テストコード作成（Red）
2. 本実装（Green）
3. リファクタリング
4. `go test ./...` 全パス確認
5. コミット & プッシュ → PR を Ready for Review に

### Step 5: E2E + README

**担当**: 仕上げ担当 (Sonnet)

1. E2Eテスト実行
2. README.md 更新（該当機能の動作確認方法）
3. コミット & プッシュ → `go test ./...` 確認

### Step 6: 全体レビュー

**担当**: Copilot

**観点**: 仕様準拠、コード品質、テスト網羅性、セキュリティ、要件トレーサビリティ表の完全性

LGTM後、修正あればコミット & プッシュ → `go test ./...` 確認

### Step 7: リリース

1. **チェックリスト確認**（漏れあれば該当ステップに戻る）
2. worktree削除 → PRマージ → ブランチ削除 → main最新化
3. 進捗ファイル更新（status: completed）→ TODO.md を `🚧` → `[x]`
4. コミット & プッシュ → GitHub Release作成 → 完了通知

```bash
git worktree remove {path} --force
gh pr merge {PR番号} --squash --delete-branch
git branch -D {ブランチ名}; git push origin --delete {ブランチ名} 2>/dev/null || true
git pull origin main
gh release create v0.{phase}.{task} --title "Phase {N} Task {M}: {タスク名}" --notes "..."
terminal-notifier -title "Dev Flow" -message "Phase{N} Task{M} 完了" -sound default
```

---

## 進捗ファイル形式

```json
{"phase":1,"task":1,"taskName":"タスク名","currentStep":4,"status":"in_progress","branch":"feature/phase1-task1","worktree":"../embedding_mcp-phase1-task1"}
```

---

## エージェント呼び出し

```bash
claude --model opus --agent .claude/agents/designer.md -p "{指示}"       # 設計者
claude --model sonnet --agent .claude/agents/implementer.md -p "{指示}"  # 実装者
claude --model sonnet --agent .claude/agents/finisher.md -p "{指示}"     # 仕上げ担当
claude --model opus --agent .claude/agents/tech-leader.md -p "{指示}"    # テックリーダー
copilot --model gpt-5.2-codex --add-dir . --allow-all-tools -p "{レビュープロンプト}"  # Copilot
```

---

## エージェント一覧

| 役割 | ファイル | モデル |
|------|----------|--------|
| 設計者 | `.claude/agents/designer.md` | Opus 4.5 |
| 実装者 | `.claude/agents/implementer.md` | Sonnet 4.5 |
| 仕上げ担当 | `.claude/agents/finisher.md` | Sonnet 4.5 |
| テックリーダー | `.claude/agents/tech-leader.md` | Opus 4.5 |
| レビュワー | (Copilot) | GPT-5.2-Codex |

---

## ユーザー確認タイミング

1. タスク開始時: どのタスクを開始/再開するか
2. レビュー指摘時: 対応方針
3. エスカレーション時: 問題の対応方針
4. 切り戻し発生時: 漏れが見つかった場合

**注意**: マージ＆リリースは全体レビューLGTM後、自動実行。

---

## 参照ドキュメント

- TODO: `TODO.md`
- 要件仕様: `requirements/embedded_spec.md`
- Skill仕様: `requirements/embedded_skill_spec.md`
